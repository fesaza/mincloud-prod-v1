@model IEnumerable<ICC.Mincloud.Modelo.Entities.LideresMinisterios>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Ministerio.Nombre)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Persona.Nombre)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.LiderGeneral)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Ministerio.Nombre)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Persona.Nombre)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.LiderGeneral)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.EncargadosMinisterioId }) |
            @Html.ActionLink("Details", "Details", new { id=item.EncargadosMinisterioId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.EncargadosMinisterioId })
        </td>
    </tr>
}

</table>
