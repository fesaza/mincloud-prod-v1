@{
    Layout = "~/Views/Shared/_LayoutPopup.cshtml";


}
@model ICC.Mincloud.Modelo.Entities.LideresMinisterios

@using (Html.BeginForm("EditarMinisterio", "Personas", FormMethod.Post, new { @onsubmit = "onSubmitCompleted" }))
{
    @Html.ValidationSummary(true)
    <fieldset class="col-md-12">
        @{ Html.HiddenFor(m => m.EncargadosMinisterioId);}
        @{ Html.RenderPartial("_CreateOrEditMinisterio");    }

        <div class="row">
            <div class="col-md-3 col-md-offset-4">
                <input type="submit" value="Guardar" class="btn btn-success" />

            </div>
        </div>
    </fieldset>
            }

<script>
    function onSubmitCompleted(args) {
        debugger;
        //$(this).closest("[data-role=window]").kendoWindow("close");
    }
</script>
