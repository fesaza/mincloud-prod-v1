@model IEnumerable<ICC.Mincloud.Modelo.Entities.Grupo>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Materia.Nombre)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Periodo.Nombre)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Tutor.TutorId)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Materia.Nombre)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Periodo.Nombre)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Tutor.TutorId)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.GrupoId }) |
            @Html.ActionLink("Details", "Details", new { id=item.GrupoId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.GrupoId })
        </td>
    </tr>
}

</table>
